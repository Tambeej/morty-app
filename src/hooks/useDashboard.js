/**
 * useDashboard hook
 * Convenience re-export from DashboardContext.
 * Provides: summary, financialProfile, comparisonData, recentOffers,
 * isLoading, error, lastFetched, fetchDashboard, clearError.
 */
export { useDashboard } from '../context/DashboardContext';
