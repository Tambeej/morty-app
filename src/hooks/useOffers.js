/**
 * useOffers hook
 * Convenience re-export from OffersContext.
 * Provides: offers, pagination, stats, isLoading, isUploading, uploadProgress,
 * error, uploadError, fetchOffers, uploadNewOffer, removeOffer, fetchStats,
 * clearError, clearUploadError.
 */
export { useOffers } from '../context/OffersContext';
