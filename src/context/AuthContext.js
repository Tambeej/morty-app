/**
 * AuthContext.js — re-exports from AuthContext.jsx.
 *
 * Kept for backward compatibility with imports that use the .js extension.
 * The canonical implementation lives in AuthContext.jsx.
 */
export { AuthProvider, useAuth, default } from './AuthContext.jsx';
